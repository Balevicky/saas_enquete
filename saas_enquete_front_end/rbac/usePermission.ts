// import { ROLE_PERMISSIONS } from "./permissions";
// import { useAuth } from "../auth/useAut";

// export function usePermission(permission: string) {
//   const { user } = useAuth();

//   if (!user?.role) return false;

//   return ROLE_PERMISSIONS[user.role]?.includes(permission);
// }
